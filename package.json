{
    "name": "birthday-monorepo",
    "private": true,
    "scripts": {
        "dev:backend": "pnpm -F backend dev",
        "dev:frontend": "pnpm -F frontend dev",
        "dev": "pnpm -r dev",
        "test": "pnpm -r test",
        "build": "pnpm -r build",
        "deploy:backend": "pnpm -F backend deploy"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}